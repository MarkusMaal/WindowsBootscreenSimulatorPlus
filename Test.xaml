<UserControl x:Class="WindowsBootscreenSimulatorPlus.Test"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WindowsBootscreenSimulatorPlus"
      DataContext="{Binding RelativeSource={RelativeSource Self}}"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <Label Name="Label1" Content="{Binding Text}" FontSize="20" HorizontalAlignment="Center" />
        <Label HorizontalAlignment="Center">This is a dummy page with no content...</Label>
    </StackPanel>
</UserControl>
